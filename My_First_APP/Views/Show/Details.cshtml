@model IEnumerable<My_First_APP.CurrentCharacteristicsValue>

@{
   // ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



<div>
    <h4>Бланк "@ViewBag.Title"</h4> 
    
    
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Characterisitcs.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Value)
            </th>            

        </tr>

        @{string old_name = "";}
        @foreach (var item in Model)
        {
            <tr>
                @if (old_name == item.Characterisitcs.Name)
                {

                <td>
                </td>
    <td>
        @Html.DisplayFor(modelItem => item.Value)
    </td>
                }
                else
                {

                    <td>
                        @Html.DisplayFor(modelItem => item.Characterisitcs.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Value)
                    </td>
                
                }
            
            @{old_name = item.Characterisitcs.Name;}
            
                <td>
                    <div class="btn btn-info">
                        @Html.ActionLink("Редактировать", "EditChar", new { id = item.Id })
                    </div>
</td>
            </tr>


        }
    </table>
   
</div>
<p>
   
    <div class="btn btn-success">
        @Html.ActionLink("Обратно к списку", "Index")
    </div>
</p>
